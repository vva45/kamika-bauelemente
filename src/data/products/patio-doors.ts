/**
 * Las puertas de terraza ya NO llevan fichas de ejemplo.
 *
 * La gama aguantaba con dos modelos inventados (un HST y un PSK
 * plausibles) hasta que llegara catálogo. Ha llegado: el folleto de
 * sistemas Salamander trae los tres sistemas correderos reales
 * —evolutionDrive SF, evolutionDrive Plus+ y evolutionDrive 82 HST—
 * y la gama pasa a ir por fabricante, igual que las ventanas.
 * Ver la entrada de Salamander con `category: "patio-doors"` en
 * `src/data/manufacturers.ts`.
 *
 * Los dos ejemplos se fueron enteros, con sus imágenes: junto a
 * sistemas reales, un ejemplo es indistinguible de un dato, y la regla
 * de la casa es que aquí no hay nada inventado.
 */
import type { Product } from "../types";

export const patioDoors: Product[] = [];
